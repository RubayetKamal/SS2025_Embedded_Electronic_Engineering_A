name: Build All LaTeX Documents

on:
  push:
    paths:
      - '**.tex'
      - '**.bib'
  workflow_dispatch:

jobs:
  build-all:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install LaTeX tools
        run: sudo apt-get update && sudo apt-get install -y texlive-latex-extra latexmk

      - name: Compile all LaTeX files
        run: |
          mkdir -p Paper_pdf
          COMPILED_COUNT=0
          IFS=$'\n' # Ensure filenames with spaces are handled
          for file in $(find . -name "*.tex"); do
            echo "🛠 Compiling \"$file\"..."
            latexmk -pdf -shell-escape -interaction=nonstopmode -output-directory="$(dirname "$file")" "$file"
            PDF_NAME="$(basename "$file" .tex).pdf"
            PDF_PATH="$(dirname "$file")/$PDF_NAME"

            if [ -f "$PDF_PATH" ]; then
              echo "✅ Compiled: $PDF_NAME"
              mv "$PDF_PATH" Paper_pdf/
              COMPILED_COUNT=$((COMPILED_COUNT + 1))
            else
              echo "❌ Failed to compile \"$file\""
            fi
          done

          echo "Compiled $COMPILED_COUNT PDFs."
          if [ "$COMPILED_COUNT" -eq 0 ]; then
            echo "🚫 No PDFs compiled. Exiting."
            exit 1
          fi

      - name: Upload all PDFs
        uses: actions/upload-artifact@v4
        with:
          name: compiled-pdfs
          path: Paper_pdf/

      - name: Commit PDFs to repository
        if: success()
        uses: EndBug/add-and-commit@v9
        with:
          author_name: "GitHub Actions"
          author_email: "actions@github.com"
          message: "Add compiled PDFs"
          add: "Paper_pdf"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
